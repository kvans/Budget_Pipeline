CREATE TABLE IF NOT EXISTS true_category (
    name                   VARCHAR(255),
    category               VARCHAR(255),
    subcategory            VARCHAR(255),
    updatedCategory        VARCHAR(255),
    updatedSubcategory     VARCHAR(255),
    insert_date            TIMESTAMP
);
